
@model ASOL.HireThings.Model.WebApiUserSearchViewModel
@{
    ViewBag.Title = "User";
}
@using (Html.BeginForm("IndexEdit", "WebApiUser", FormMethod.Post, new { id = "UserSearch" }))
{
    @Html.Hidden("hdnWAUserId")
    <div class="row">
        <div class="col-lg-12">
            <div class="panel CustomSearchBox">
                <div class="panel-body">
                    <div class="">
                        <div class="form-group">
                            <div class="col-sm-6">
                                <label for="@Model.DDLSystem[0].Identifier" class="control-label">Organization</label>

                                @Html.Partial("~/Views/Shared/_Partial/_ddlWithClick.cshtml", Model.DDLSystem)
                            </div>                            
                        </div>
                        <div class="form-group">
                            <div class="col-sm-6">
                                <label for="txtSearch" class="control-label">Search</label>
                                <input type="text" class="form-control" id="txtSearch" />
                            </div>
                            <div class="col-sm-6">
                                <label for="ddlSearch" class="control-label">Search Field</label>
                                @Html.DropDownListFor(m => m.SearchId, new SelectList(Model.DDLSearch, "Value", "Text"), new { @id = "ddlSearch", @class = "form-control" })
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="col-sm-6">
                                <span class="visible-md visible-sm visible-xs">&nbsp;</span>
                                <input type="button" class="btn btn-primary" id="btnWAUserSearch" value="Search" />
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div id="DivUserGrid">
        @Html.Partial("_UserGrid", Model.UserList)
    </div>
    <script src="~/Scripts/AppJS/webApiUser/hirethings.webapiuser.search.js"></script>

    <script>
        $(document).ready(function () {
            HIRETHINGS.UI.User.Search.initialisePage();

        });
    </script>

}


